import 'package:flutter/material.dart';

class TabelsList extends StatelessWidget {
  const TabelsList({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: const Center(child: Text('Tavoli')),
    );
  }
}
